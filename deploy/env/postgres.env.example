# Server-only PostgreSQL bootstrap env. Replace every placeholder on the VPS.
# This file is an example only; do not copy real values into Git.

POSTGRES_USER=algoedgefno_postgres_admin
POSTGRES_PASSWORD=replace-with-strong-postgres-admin-password
POSTGRES_DB=postgres
