// Package providers contains concrete implementations of models.MarketDataProvider.
// All interface types (MarketDataProvider, Capability, ProviderStatus, etc.) live in
// internal/models — providers depend on models, not the other way around.
package providers
